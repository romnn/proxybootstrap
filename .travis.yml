language: python
cache:
  pip: true
  directories:
    - $HOME/.cache/pre-commit
install:
  - pip install -e .
deploy:
  provider: pypi
  user: "__token__"
  password:
    secure: "Your encrypted token"
  on:
    tags: true
    branch: master